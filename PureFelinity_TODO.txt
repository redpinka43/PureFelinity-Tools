 For the future, if you want to install a module for your version of Python (3.11), use:
	py -3.11 -m pip install [module_name]

To run the script, run 
	py match_cats.py
	or 
	py find_a_date.py


-----

Next features:
- Fix white(rufused) cats from having the coat color tracked as 'white(shows'